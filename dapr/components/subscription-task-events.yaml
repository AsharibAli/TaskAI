# Dapr subscription for task-events topic
apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: task-events-subscription
spec:
  pubsubname: kafka-pubsub
  topic: task-events
  routes:
    default: /api/events/task
scopes:
  - taskai-recurring-service
